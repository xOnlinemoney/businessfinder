import { MediaContent } from './media-content';

export const metadata = {
  title: 'Media Library | Admin - BusinessFinder',
  description: 'Manage uploaded media files',
};

export default function MediaPage() {
  return <MediaContent />;
}
