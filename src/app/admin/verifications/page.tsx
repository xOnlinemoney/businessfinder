import { VerificationsContent } from './verifications-content';

export const metadata = {
  title: 'Verifications | Admin - BusinessFinder',
  description: 'Review and approve business verifications',
};

export default function VerificationsPage() {
  return <VerificationsContent />;
}
